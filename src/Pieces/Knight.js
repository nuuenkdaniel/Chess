class Knight extends Piece{
    constructor(color, tileX, tileY, board){
        super(color, tileX, tileY, board);
    }

    getMoveInfo(){
        let possibleMoves = [];
        let moveY = 0;
        let moveX = 0;
        
    }
}